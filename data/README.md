# Data Files

Most of the data files here are going to be OSM (OpenStreetMap) files with routes.
The raw data and the munged output are both stored here. Typically the munged output
only needs to be generated once-per-region, unless additional OSM files are added.
